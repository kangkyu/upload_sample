# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mam_dev?sslmode=disable

# Google Cloud Storage
GCS_BUCKET=upload_sample_bucket

# Server
PORT=8080

# YouTube API (OAuth 2.0 Web Application)
YOUTUBE_CLIENT_ID=your-client-id.apps.googleusercontent.com
YOUTUBE_CLIENT_SECRET=your-client-secret

# Encryption key for storing OAuth tokens (32 bytes, base64 encoded)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-base64-encoded-32-byte-key
